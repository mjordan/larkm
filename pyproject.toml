[tool.setuptools.packages.find]
exclude = ["extras", "fixtures"]
namespaces = false

[project]
name = "larkm"
version = "1.0.0"
description = "A lightweight ARK manager"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "fastapi",
    "uvicorn",
    "Whoosh",
    "pytest"
]
